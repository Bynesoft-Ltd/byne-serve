setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: backend/Dockerfile
    worker: frontend/Dockerfile
run:
  web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
  worker: serve -s build -l $PORT